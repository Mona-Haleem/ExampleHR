# Port the main service runs on
PORT=3000

# Base URL of the HCM system (or mock server)
HCM_BASE_URL=http://localhost:3001

# Path to SQLite database file (use :memory: for testing)
DATABASE_PATH=./data/timeoff.sqlite
